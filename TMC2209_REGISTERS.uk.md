*[English version](TMC2209_REGISTERS.md)*

# Довідник по регістрах TMC2209

Кожен регістр, який `tmc2209.py` реально читає чи записує, навіщо, і що означають відповідні біти. Це не повний переклад даташиту — лише те, чого торкається цей код. Для всього іншого дивіться офіційний даташит:

**[TMC2209 Datasheet, Rev. 1.09](https://www.analog.com/media/en/technical-documentation/data-sheets/tmc2209_datasheet_rev1.09.pdf)** (Analog Devices / Trinamic Motion Control). Номери розділів нижче (наприклад, "5.5.3") посилаються саме на цей документ.

## Протокол датаграм (як `tmc2209.py` спілкується з чіпом)

Усі 4 вбудовані драйвери діляться однією шиною UART, розрізняються за адресою вузла (0-3), яка задається strapping'ом MS1/MS2 кожного чіпа. Розділ 5 даташиту, "UART Single Wire Interface":

- **Запис** (8 байт): `sync(0x05) | addr | reg|0x80 | data[3] | data[2] | data[1] | data[0] | CRC8`
- **Запит читання** (4 байти): `sync(0x05) | addr | reg&0x7F | CRC8`, чіп відповідає 8-байтною датаграмою: `sync(0x05) | 0xFF(адреса майстра) | reg | data[3..0] | CRC8`
- CRC8 використовує поліном 0x07 (з реверсом бітів), реалізовано в `_crc8()`.
- На цій платі (BTT SKR Pico) TX з'єднаний з RX через резистор, тож **кожен відправлений байт повертається як ехо** і має бути вичитаний перед реальною відповіддю — див. `TMC2209Bus._flush_rx()`/`_read_exact()`.

## Таблиця регістрів

| Адр | Регістр | Доступ | Використання |
|---|---|---|---|
| 0x00 | GCONF | R+W | Налаштування режиму UART (`pdn_disable`, `mstep_reg_select`), вибір stealthChop/spreadCycle |
| 0x01 | GSTAT | R+WC | Стан драйвера: прапори reset/перегрів-або-КЗ/недостатня напруга — `diag_summary()` |
| 0x06 | IOIN | R | Перевірка версії чіпа (`check_connection()`) |
| 0x10 | IHOLD_IRUN | **W (тільки запис)** | Робочий/утримуючий струм (`set_current()`) |
| 0x13 | TPWMTHRS | W | Оголошено, але наразі не використовується цим кодом |
| 0x14 | TCOOLTHRS | **W (тільки запис)** | Мінімальний поріг швидкості StallGuard/CoolStep (`enable_stallguard()`) |
| 0x40 | SGTHRS | **W (тільки запис)** | Поріг застою StallGuard (`enable_stallguard()`) |
| 0x41 | SG_RESULT | R | Живий показник навантаження StallGuard, опитується під час `HOME` (`read_stallguard_result()`) |
| 0x6C | CHOPCONF | R+W | Роздільність мікрокроку / поле MRES (`set_microsteps()`, `read_microsteps()`) |
| 0x6F | DRV_STATUS | R | Прапори несправностей, жива шкала струму, режим, standstill — `diag_summary()` |
| 0x70 | PWMCONF | R+W | Біти `pwm_autoscale`/`pwm_autograd` для stealthChop (`enable_uart_mode()`) |

**R+WC** = читання, запис-очищення: запис `1` в біт очищує його (використовується для GSTAT — див. `clear_gstat()`).

### Регістри "тільки запис": не намагайтесь перевіряти їх читанням

IHOLD_IRUN, TCOOLTHRS та SGTHRS позначені **W** в таблиці доступу до регістрів даташиту — чіп не повертає їхній вміст при читанні, незалежно від того, чи запис пройшов успішно. Читання будь-якого з них завжди повертає `0x00000000`. Це збило з пантелику попередню сесію діагностики в цьому проєкті (задокументовано в [TROUBLESHOOTING.md](TROUBLESHOOTING.md)): читання `0x00000000` для цих трьох було помилково прийняте за невдалий запис, хоча записи насправді весь час проходили успішно. Лише GCONF/CHOPCONF/PWMCONF (усі R+W) мають сенс для перевірки читанням.

Саме тому `sgthrs=N(cfg)` в `X/Y/Z TMC` в `scanner_rig.py` позначено `(cfg)`: це те значення, яке програма останнім записала в регістр, а не те, що зчитано з чіпа.

## Біти GCONF (0x00), що використовуються тут

| Біт | Назва (як у цьому коді) | Значення |
|---|---|---|
| 2 | `en_spreadcycle` (параметр `spreadcycle` у `enable_uart_mode()`) | 0 = stealthChop (тихий, для звичайного циклічного руху), 1 = spreadCycle (лише під час `HOME`, де StallGuard потребує чистого сигналу) |
| 6 | `pdn_disable` | Має бути встановлений для роботи UART (звільняє пін PDN_UART від ролі pin-down/аналогового вибору) |
| 7 | `mstep_reg_select` | Має бути встановлений, щоб роздільність мікрокроку бралась з поля MRES регістра CHOPCONF (керування регістром), а не з пінів MS1/MS2 |

## Біти GSTAT (0x01) — таблиця "general configuration registers" даташиту

| Біт | Назва | Значення |
|---|---|---|
| 0 | `reset` | Чіп був скинутий з моменту останнього читання GSTAT — очікувано одразу після подачі живлення |
| 1 | `drv_err` | Драйвер вимкнено через перегрів або коротке замикання (див. DRV_STATUS, яке саме) |
| 2 | `uv_cp` | Недостатня напруга на charge pump — драйвер вимкнено, поки цей біт встановлено |

## Біти DRV_STATUS (0x6F) — розділ 5.5.3 даташиту

| Біт | Назва | Значення |
|---|---|---|
| 0 | `otpw` | Попередження про перегрів |
| 1 | `ot` | Вимкнення через перегрів |
| 2 | `s2ga` | Коротке замикання на землю, фаза A |
| 3 | `s2gb` | Коротке замикання на землю, фаза B |
| 4 | `s2vsa` | Коротке замикання на низькій стороні, фаза A |
| 5 | `s2vsb` | Коротке замикання на низькій стороні, фаза B |
| 6 | `ola` | Обрив навантаження, фаза A (лише інформативно — даташит зазначає хибні спрацювання під час швидкого руху/спокою; перевіряйте на повільному русі) |
| 7 | `olb` | Обрив навантаження, фаза B (те саме застереження) |
| 8-11 | `t120`/`t143`/`t150`/`t157` | Компаратори температурних порогів — не декодуються `diag_summary()` (otpw/ot покривають практично корисні випадки) |
| 16-20 | `cs_actual` | Жива шкала струму (0-31), яка реально застосовується — конвертується в мА через `cs_to_ma()`, обернену до формули `set_current()`. Це шкала `IHOLD`, поки `standstill=1`, і `IRUN` — під час руху, а не просто "те, з чим останнього разу викликали `set_current()`" |
| 30 | `stealth` | 1 = активний stealthChop, 0 = spreadCycle |
| 31 | `stst` | Standstill: мотор нерухомий вже 2^20 тактів |

## Розкладка IHOLD_IRUN (0x10)

| Біти | Поле | Примітки |
|---|---|---|
| 4:0 | `IHOLD` | Шкала утримуючого струму (0-31), застосовується в стані спокою |
| 12:8 | `IRUN` | Шкала робочого струму (0-31), застосовується під час руху |
| 19:16 | `IHOLDDELAY` | Затримка (в одиницях ~2^18 тактів) перед переходом з IRUN на IHOLD після зупинки руху |

Формула шкали струму (CS), яку використовують `set_current()`/`cs_to_ma()` (`_ma_to_cs`/`cs_to_ma` в `tmc2209.py`) — стандартна формула Trinamic:

```
CS = round(32 * sqrt(2) * I_rms_amps * (Rsense + 0.02) / Vfs - 1)   # обмежено 0-31
Vfs = 0.180 якщо vsense, інакше 0.325
```

`Rsense`/`vsense` — аргументи конструктора `TMC2209` (за замовчуванням `rsense=0.11, vsense=False`, що відповідає вбудованим драйверам BTT SKR Pico) — не захардкоджені у формулу, на випадок якщо колись знадобиться інша плата/конфігурація токовимірювального резистора.

## CHOPCONF (0x6C): поле MRES (біти 27:24)

`set_microsteps()`/`read_microsteps()` торкаються лише цього поля, зберігаючи всі інші біти CHOPCONF (read-modify-write):

| MRES | Мікрокроків |
|---|---|
| 0 | 256 |
| 1 | 128 |
| 2 | 64 |
| 3 | 32 |
| 4 | 16 (дефолт цього рига — див. `MICROSTEPS` в `scanner_rig.py`) |
| 5 | 8 |
| 6 | 4 |
| 7 | 2 |
| 8 | 1 (повний крок) |

## Біти PWMCONF (0x70), що використовуються тут

| Біт | Назва | Значення |
|---|---|---|
| 18 | `pwm_autoscale` | Контур регулювання струму stealthChop підлаштовує амплітуду ШІМ під реальний мотор/навантаження, замість роботи "розімкнено" на заводських значеннях |
| 19 | `pwm_autograd` | Автоматичне налаштування градієнта ШІМ, використовується разом з `pwm_autoscale` |

Обидва встановлюються `enable_uart_mode()` через read-modify-write, залишаючи заводські `pwm_freq`/`pwm_grad`/`pwm_ofs` незмінними.
